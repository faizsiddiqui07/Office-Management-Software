# Dependencies
node_modules/
.pnp
.pnp.js

# Builds
dist/
build/
.next/
out/

# Env
.env
.env.local
.env.*.local
*/.env
!*/.env.example

# Uploads (runtime files)
backend/uploads/*
!backend/uploads/.gitkeep

# Lambda deploy package + cloned skill repos (not part of the app source)
*.zip
backend/lambda.zip
skills-repo/

# Written by scripts/package-lambda.mjs from the commit it packaged, so /api/health
# can report which build is live. Generated, never committed.
backend/src/build-info.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Caches / tooling
.turbo/
.cache/
coverage/
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code local files (not part of the app)
.claude/
